/**
 * 
 * Enum que servirá para los formularios.
 * 
 */
export enum TypeForm {
    CREATE = 'CREATE',
    UPDATE = 'UPDATE',
    UPDATE_PASSWORD = 'UPDATE_PASSWORD',
    READ = 'READ',
    REPORT =  'REPORT'
}