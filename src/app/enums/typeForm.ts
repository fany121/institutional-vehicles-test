/**
 * 
 * Enum que servirá para los formularios.
 * 
 */
export enum TypeForm {
    CREATE = 'CREATE',
    UPDATE = 'UPDATE',
    UPDATE_PASSWORD_USER = 'UPDATE_PASSWORD_USER',
    UPDATE_PASSWORD_ME = 'UPDATE_PASSWORD_ME',
    READ = 'READ',
    REPORT =  'REPORT'
}